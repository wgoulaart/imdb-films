import styled from 'styled-components'

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  gap: var(--size-base);
  font-weight: 700;
`
export const BadgeImdb = styled.div`
  background-color: var(--color-imdb);
  color: #000;
  padding: var(--size-tiny) var(--size-base);
  border-radius: var(--size-tiny);
  font-size: 14px;
  font-weight: 600;
`
